// Prologue:
const {print, spawn, sleep, scheduler} = require('./runtime');

let data = [1, 2, 3];
data[0];
print(data, data[0]);
let z = 3;
let point = {x: 1, y: 2, z: z};
point.x;
print(point, point.x);
        